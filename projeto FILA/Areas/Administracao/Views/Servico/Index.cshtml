@model PagedList.IPagedList<projeto_FILA.Models.Servico>
@using PagedList
@using PagedList.Mvc
@{
    ViewBag.Title = "Serviços";
}

<h2>Serviços</h2>
<form method="get" action="/Administracao/Servico/Consultar">
    <p>
        <input type="search" name="NomeF" />
        <input type="submit" value="Pesquisar" /> <input type="submit" value="Mostrar Todos" />
    </p>
</form>

@*<div class="panel panel-body">
    <div class="panel-heading">
        <div class="panel-title">Filtrar</div>
        <div class="panel-body">

            <div class="col-lg-4">
                <label for="TipoServico">Tipo de Serviço:</label>
                <select id="TipoServico">
                    <option>Barba</option>
                    <option>Corte</option>
                    <option>Sombrançelha</option>
                </select>
                <button>Filtrar</button>
            </div>

            <div class="col-lg-8">
                <label for="Periodo">Período:</label>
                Início: <input type="text" name="DataInicio" /> Fim: <input type="text" name="DataFim" /> 
                <button>Filtrar</button>
            </div>

        </div>
    </div>
</div>*@


<p>
    <a class="btn icon-btn btn-success" href="/Administracao/Servico/Create"><span class="glyphicon btn-glyphicon glyphicon-plus img-circle text-success"></span>Adicionar</a>
    @*@Html.ActionLink("Create New", "Create")*@
</p>

@*@Html.ActionLink("Home", "Index", "home")*@
<table class="table">
    <tr>
        <th>
            @Html.ActionLink("Serviço", "Index", new { ordenacao = ViewBag.NomeParam })
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeServico)
        </td>
        <td>
            @*<button class="btn btn-info"> <i class="glyphicon glyphicon-edit"></i></button>
        <button class="btn btn-warning"><i class="glyphicon glyphicon-eye-open"></i></button>
        <button class="btn btn-danger"><i class="glyphicon glyphicon-remove-circle"></i></button>*@
            <a href="@Url.Action("Edit", new { id=item.ServicoID })" title="Editar" class="btn btn-info"><i class="glyphicon glyphicon-edit"></i></a>
            <a href="@Url.Action("Details", new { id=item.ServicoID })" title="Visualizar" class="btn btn-warning"><i class="glyphicon glyphicon-eye-open"></i></a>
            <a href="@Url.Action("Delete", new { id=item.ServicoID })" title="Deletar" class="btn btn-danger"><i class="glyphicon glyphicon-remove-circle"></i></a>

        </td>
    </tr>
}

</table>
Página @Model.PageNumber de @Model.PageCount
@Html.PagedListPager(Model, Page => Url.Action("Index", new { pagina = Page, ordenacao = ViewBag.OrdenacaoAtual })) 